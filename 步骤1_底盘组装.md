# 步骤1：底盘组装 + 电机转起来（Day 1-2）

## 目标
把底盘拼好，4个电机能独立正反转、调速。

---

## 1.1 认识你的硬件

### 4WD 铝合金底盘包含：
```
  [前左轮] ←编码器→ [前右轮]
     │                  │
     ├─ 电机A ─── 电机B ─┤
     │                  │
  [后左轮] ←编码器→ [后右轮]
```

- 4个带编码器的直流减速电机（通常 6V 170RPM）
- 编码器线：红(VCC) 黑(GND) 黄(A相) 绿(B相)
- 电机线：红(+) 黑(-)

### TB6612 电机驱动板 ×2：
```
每个 TB6612 控制 2 个电机：

VM  → 12V (电池+)
VCC → 5V  (逻辑供电)
GND → 电池- / 树莓派 GND 共地

AIN1 → Arduino 数字脚 (方向1)
AIN2 → Arduino 数字脚 (方向2)
PWMA → Arduino PWM脚  (速度)
AO1  → 电机A+
AO2  → 电机A-

BIN1 → Arduino 数字脚 (方向1)
BIN2 → Arduino 数字脚 (方向2)
PWMB → Arduino PWM脚  (速度)
BO1  → 电机B+
BO2  → 电机B-
```

---

## 1.2 接线图

### Arduino Mega 引脚分配：

```
电机控制 (TB6612 #1, 前轮):
  前左电机: AIN1=22, AIN2=23, PWMA=6
  前右电机: BIN1=24, BIN2=25, PWMB=7

电机控制 (TB6612 #2, 后轮):
  后左电机: AIN1=26, AIN2=27, PWMA=8
  后右电机: BIN1=28, BIN2=29, PWMB=9

编码器 (中断脚):
  前左: A=18, B=19
  前右: A=20, B=21
  后左: A=2,  B=3
  后右: A=4,  B=5
```

### 电源接线：
```
  ┌──────────┐
  │12V锂电池 │
  │  +   -   │
  └──┬───┬───┘
     │   │
     │   └──→ LM2596#1 → 5V → 树莓派5 (USB-C PD口)
     │                         
     │   ┌──→ LM2596#2 → 5V → Arduino Mega (5V脚)
     │   │                     
     └───┼──→ TB6612#1 VM (12V)
         │
         └──→ TB6612#2 VM (12V)
         
     所有 GND 连在一起！（共地）
```

---

## 1.3 组装步骤

### 步骤A：安装电机和轮子
1. 把 4 个电机用螺丝固定在底盘电机座上
2. 装上联轴器（电机轴→轮轴）
3. 装好 4 个轮子，拧紧
4. 确认所有轮子能自由转动

### 步骤B：固定电路板
1. 用铜柱把 Arduino Mega 固定在底盘上层
2. 固定 2 个 TB6612 驱动板
3. 固定 2 个 LM2596 降压模块
4. 用扎带固定电池

### 步骤C：接线
1. **先不接电池**，所有开关关闭
2. 按照 1.2 的引脚表，用杜邦线接好电机线
3. 接好 TB6612 的电源和逻辑线
4. 接好降压模块
5. **最后接电池**（确认正负极！）

---

## 1.4 第一个程序：让轮子转

### 在 Arduino IDE 中（电脑上安装 https://www.arduino.cc/en/software）

```cpp
// test_motors.ino - 测试4个电机
// 上传到 Arduino Mega 后，打开串口监视器(115200)

// 前左
#define FL_IN1 22
#define FL_IN2 23
#define FL_PWM 6

// 前右
#define FR_IN1 24
#define FR_IN2 25
#define FR_PWM 7

// 后左
#define BL_IN1 26
#define BL_IN2 27
#define BL_PWM 8

// 后右
#define BR_IN1 28
#define BR_IN2 29
#define BR_PWM 9

void setup() {
  Serial.begin(115200);
  
  // 所有电机脚设为输出
  pinMode(FL_IN1, OUTPUT); pinMode(FL_IN2, OUTPUT);
  pinMode(FR_IN1, OUTPUT); pinMode(FR_IN2, OUTPUT);
  pinMode(BL_IN1, OUTPUT); pinMode(BL_IN2, OUTPUT);
  pinMode(BR_IN1, OUTPUT); pinMode(BR_IN2, OUTPUT);
  
  Serial.println("=== 电机测试 ===");
  Serial.println("输入命令:");
  Serial.println("  w = 前进");
  Serial.println("  s = 后退");
  Serial.println("  a = 左转");
  Serial.println("  d = 右转");
  Serial.println("  x = 停止");
  Serial.println("  数字0-9 = 速度 (0=停 9=最快)");
}

int speed_val = 150; // 默认速度 0-255

void loop() {
  if (Serial.available()) {
    char cmd = Serial.read();
    
    if (cmd >= '0' && cmd <= '9') {
      speed_val = map(cmd - '0', 0, 9, 0, 255);
      Serial.print("速度设为: "); Serial.println(speed_val);
    }
    
    switch (cmd) {
      case 'w': goForward();  break;
      case 's': goBackward(); break;
      case 'a': turnLeft();   break;
      case 'd': turnRight();  break;
      case 'x': stopAll();    break;
    }
  }
}

// ===== 基础动作 =====

void setMotor(int in1, int in2, int pwm, int speed) {
  digitalWrite(in1, speed > 0 ? HIGH : LOW);
  digitalWrite(in2, speed < 0 ? HIGH : LOW);
  analogWrite(pwm, abs(speed));
}

void goForward() {
  Serial.println("前进 ↑");
  // 全部正转
  setMotor(FL_IN1, FL_IN2, FL_PWM, speed_val);
  setMotor(FR_IN1, FR_IN2, FR_PWM, speed_val);
  setMotor(BL_IN1, BL_IN2, BL_PWM, speed_val);
  setMotor(BR_IN1, BR_IN2, BR_PWM, speed_val);
}

void goBackward() {
  Serial.println("后退 ↓");
  setMotor(FL_IN1, FL_IN2, FL_PWM, -speed_val);
  setMotor(FR_IN1, FR_IN2, FR_PWM, -speed_val);
  setMotor(BL_IN1, BL_IN2, BL_PWM, -speed_val);
  setMotor(BR_IN1, BR_IN2, BR_PWM, -speed_val);
}

void turnLeft() {
  Serial.println("左转 ←");
  setMotor(FL_IN1, FL_IN2, FL_PWM, -speed_val);  // 左轮反转
  setMotor(BL_IN1, BL_IN2, BL_PWM, -speed_val);
  setMotor(FR_IN1, FR_IN2, FR_PWM, speed_val);   // 右轮正转
  setMotor(BR_IN1, BR_IN2, BR_PWM, speed_val);
}

void turnRight() {
  Serial.println("右转 →");
  setMotor(FL_IN1, FL_IN2, FL_PWM, speed_val);
  setMotor(BL_IN1, BL_IN2, BL_PWM, speed_val);
  setMotor(FR_IN1, FR_IN2, FR_PWM, -speed_val);
  setMotor(BR_IN1, BR_IN2, BR_PWM, -speed_val);
}

void stopAll() {
  Serial.println("停止 ■");
  digitalWrite(FL_IN1, LOW); digitalWrite(FL_IN2, LOW);
  digitalWrite(FR_IN1, LOW); digitalWrite(FR_IN2, LOW);
  digitalWrite(BL_IN1, LOW); digitalWrite(BL_IN2, LOW);
  digitalWrite(BR_IN1, LOW); digitalWrite(BR_IN2, LOW);
}
```

---

## 1.5 测试验证

1. USB 线连接电脑 → Arduino Mega
2. Arduino IDE 选择端口 → 上传
3. 打开**串口监视器**（波特率 115200）
4. 小车架空（轮子离地，防止跑走！）
5. 输入 `5` 设速度 → `w` 前进：
   - ✅ 4个轮子**同时正转** = 接线正确
   - ❌ 某个轮子反转 → 把那根电机线对调
   - ❌ 某个轮子不动 → 检查驱动板和接线

### 方向调试表：

| 现象 | 解决 |
|------|------|
| 某个轮转向反了 | 对调该电机的两根线 |
| 命令w但走歪了 | 调整速度补偿 |
| 电机抖动不转 | 检查PWM脚、电源 |

---

## 1.6 编码器测试

```cpp
// 添加到上面的代码中
// 编码器中断计数

volatile long enc_FL = 0, enc_FR = 0;
volatile long enc_BL = 0, enc_BR = 0;

void encFL() { enc_FL++; }
void encFR() { enc_FR++; }
void encBL() { enc_BL++; }
void encBR() { enc_BR++; }

void setup() {
  // ... 前面的代码 ...
  
  // 编码器中断
  attachInterrupt(digitalPinToInterrupt(18), encFL, RISING);
  attachInterrupt(digitalPinToInterrupt(20), encFR, RISING);
  attachInterrupt(digitalPinToInterrupt(2),  encBL, RISING);
  attachInterrupt(digitalPinToInterrupt(4),  encBR, RISING);
  
  Serial.println("编码器就绪，输入 e 查看读数");
}

void loop() {
  // ... 前面的代码 ...
  if (cmd == 'e') {
    Serial.print("FL:"); Serial.print(enc_FL);
    Serial.print(" FR:"); Serial.print(enc_FR);
    Serial.print(" BL:"); Serial.print(enc_BL);
    Serial.print(" BR:"); Serial.println(enc_BR);
  }
  if (cmd == 'r') {
    // 重置编码器
    enc_FL = enc_FR = enc_BL = enc_BR = 0;
    Serial.println("编码器归零");
  }
}
```

---

## ✅ Day 1-2 验收标准

- [ ] 4个轮子都能正转/反转
- [ ] 能通过串口调速（0-9）
- [ ] 前进/后退/左转/右转/停止正常
- [ ] 编码器能读数
- [ ] 车放地上能跑直线（如果跑偏，记录偏差量用于后续补偿）

### 常见问题

| 问题 | 排查 |
|------|------|
| 电机全不动 | 电池电压够吗？降压模块输出5V了吗？ |
| Arduino不响应 | USB线是数据线（不是纯充电线） |
| 某个TB6612发烫 | 短路！立即断电检查接线 |
| 编码器读数乱跳 | 加上拉电阻(10KΩ) 或 用 INPUT_PULLUP |
